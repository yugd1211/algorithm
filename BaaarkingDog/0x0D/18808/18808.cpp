#include <iostream>
#include <vector>

using namespace std;
int N, M, K;
// N = 노트북 세로
// M = 노트북 가로
// K = 스티커 개수

int board[42][42];
vector<pair<int, int> >stickers;

int main()
{
    cin >> N;
    cin >> M;
    cin >> K;
    for (int i = 0; i < K; i++)
    {
        int R; cin >> R;
        int C; cin >> C;
        for (int j = 0; j < R; j++)
        {
            for (int k = 0; k < C; k++)
            {
                
            }
        }
    }
    return 0;
}